<h2> <%= @image.title %></h2>
<%= link_to "Home", root_path %>
<%= link_to "Delete", image_path(@image), method: :delete, data: { confirm: "Are you sure you want to delete this image?" } %>
<%= image_tag @image.file.variant(resize_to_limit: [500, 500] ) %>
