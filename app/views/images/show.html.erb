<h2> <%= @image.title %></h2>

<div>
<%= link_to "Home", root_path %>
</div>

<div>
<%= link_to "Destroy", image_path(@image), method: :delete, data: { confirm: "Are you sure you want to delete this image?" } %>
</div>

<div>
<%= image_tag @image.file.variant(resize_to_limit: [500, 500] ) %>
</div>